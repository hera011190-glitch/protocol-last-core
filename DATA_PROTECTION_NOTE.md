# 데이터 보호 패치 안내

이 패치는 크롬/웨일처럼 브라우저를 바꿔 운영 페이지를 열었을 때 빈 화면 상태가 서버 데이터를 덮어써서 캐릭터/계정 데이터가 사라지는 일을 막기 위한 패치입니다.

## 포함 파일

- server.js
- client/src/AdminPage.js

## 핵심 변경

- 서버의 users.json / characters.json 저장 전에 자동 백업 생성
- 기존 데이터가 있는데 빈 배열로 저장하려는 위험한 저장 차단
- 운영 페이지가 빈 캐릭터 목록을 받았을 때 기존 화면 데이터를 바로 지우지 않음
- 운영에서 캐릭터 저장 전 서버의 최신 캐릭터 데이터를 다시 불러오고, 실패하면 저장 중단
- 운영 페이지 API 호출이 배포 환경에서도 현재 서버 기준으로 동작하도록 정리

## 백업 위치

DATA_DIR 아래 `_backups` 폴더에 자동 생성됩니다.
예: /var/data/_backups/characters.json.날짜.bak.json

