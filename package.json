{
  "name": "protocol-last-core-deploy-ready",
  "version": "1.0.0",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm --prefix client install && npm --prefix client run build"
  },
  "engines": {
    "node": ">=22.x"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "socket.io": "^4.8.1"
  }
}
