# Single-service Render deploy: usually leave this blank so the frontend uses the same origin as the server.
# Separate frontend/backend deploy: set this to your backend URL.
REACT_APP_API_BASE_URL=https://your-backend-or-site-url
